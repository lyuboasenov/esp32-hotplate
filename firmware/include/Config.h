#pragma once

/**
 * @def ROTARY_ENCODER_CLK_PIN
 * @brief The ESP32 pin connected to rotary encoder CLK pin.
 */
#ifndef ROTARY_ENCODER_CLK_PIN
#define ROTARY_ENCODER_CLK_PIN 25
#endif

/**
 * @def ROTARY_ENCODER_DT_PIN
 * @brief The ESP32 pin connected to rotary encoder DT pin.
 */
#ifndef ROTARY_ENCODER_DT_PIN
#define ROTARY_ENCODER_DT_PIN 26
#endif

/**
 * @def ROTARY_ENCODER_SW_PIN
 * @brief The ESP32 pin connected to rotary encoder SW pin.
 */
#ifndef ROTARY_ENCODER_SW_PIN
#define ROTARY_ENCODER_SW_PIN 27
#endif

/**
 * @def PIEZO_BUZZER_PIN
 * @brief The ESP32 pin connected to piezo buzzer.
 */
#ifndef PIEZO_BUZZER_PIN
#define PIEZO_BUZZER_PIN 12
#endif

/**
 * @def TEMP_SENSOR_PIN
 * @brief The ESP32 pin connected to temp sensor voltage devider.
 */
#ifndef TEMP_SENSOR_PIN
#define TEMP_SENSOR_PIN 13
#endif

/**
 * @def HEATER_SWITCH_PIN
 * @brief The ESP32 pin connected to heater switch.
 */
#ifndef HEATER_SWITCH_PIN
#define HEATER_SWITCH_PIN 14
#endif